// Greeting
// -----------------------------------------------------------------------------

// This is just a "Hello there" inline element
// which should disappear due to lack of space
// when the device is in portrait mode:
.greeting

  @media (max-width 768px - 1px) and (orientation portrait)
    display none
